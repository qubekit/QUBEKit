name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base deps
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - deepdiff

    # Core deps
  - openff-toolkit-base >=0.10.6
  - ambertools
  - pydantic
  - forcebalance
  - rdkit
  - tqdm
  - torsiondrive
  - numpy >=1.18.1
  - chargemol
  - scipy
  - click
  - networkx >=2.4
  - matplotlib-base
  - qcengine >=0.18.0
  - qcelemental >=0.19.0
  - jinja2
  - typing-extensions

  # Optional
  - xtb-python
  - torchani
  - chemper

  - pip:
      - qforce